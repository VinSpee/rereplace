{
  "name": "rereplace",
  "version": "0.2.0",
  "description": "replace regex in one file with the contents of another file",
  "main": "lib/index.js",
  "bin": "lib/cli.js",
  "scripts": {
    "pretest": "npm run lint",
    "test": "babel-tape-runner src/**/__tests__/*-test.js | tap-spec",
    "posttest": "npm run test:coverage",
    "istanbul": "^0.4.2",
    "lint": "eslint src/**/*.js",
    "test:watch": " chokidar 'src/**/*.js' -c 'clear && npm run -s test'",
    "test:coverage": "babel-node node_modules/.bin/isparta cover --report text --report html ./src/__tests__/*-test.js",
    "build": "babel src --out-dir lib",
    "watch": "babel src --out-dir lib --watch",
    "prepublish": "npm run build",
    "start": "node lib/cli.js"
  },
  "babel": {
    "presets": [
      "es2015-node5"
    ]
  },
  "keywords": [
    "regex",
    "replace",
    "inject",
    "insert"
  ],
  "author": "Vince Speelman",
  "license": "ISC",
  "dependencies": {
    "meow": "^3.7.0",
    "fs-jetpack": "^0.7.1"
  },
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-eslint": "^4.1.8",
    "babel-preset-es2015-node5": "^1.1.2",
    "babel-tape-runner": "^2.0.0",
    "chokidar-cli": "^1.2.0",
    "eslint": "^1.10.3",
    "eslint-config-airbnb": "^4.0.0",
    "isparta": "^4.0.0",
    "npm-run-all": "^1.5.1",
    "tap-spec": "^4.1.1",
    "tape": "^4.4.0"
  }
}
